/**
 * Simple horizontal divider for separating sections
 * Matches the design's subtle divider between admin sections
 */
const SectionDivider = () => {
  return <hr className="my-8 border-t border-[#EBEBEB]" />;
};

export { SectionDivider };
